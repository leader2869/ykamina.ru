import { products } from '@/lib/products';
export default function AdminPage() { return <section className="container-page py-12"><div className="flex items-end justify-between"><div><p className="text-xs uppercase tracking-[.18em] text-terracotta">Демо-режим</p><h1 className="mt-2 font-serif text-5xl">Админ-панель</h1></div><button className="rounded-full bg-terracotta px-4 py-2 text-sm text-white">+ Добавить товар</button></div><div className="mt-10 grid gap-4 sm:grid-cols-3">{[['Товары', String(products.length)], ['В наличии', String(products.reduce((sum, item) => sum + item.stock, 0))], ['Обновление цен', 'сегодня, 10:45']].map(([label, value]) => <div className="rounded-2xl bg-white p-5" key={label}><p className="text-sm text-ink/55">{label}</p><p className="mt-2 font-serif text-3xl">{value}</p></div>)}</div><div className="mt-8 overflow-hidden rounded-2xl bg-white"><div className="grid grid-cols-[1fr_auto_auto] gap-4 border-b p-5 text-sm text-ink/50"><span>Товар</span><span>Цена</span><span>Остаток</span></div>{products.map((product) => <div className="grid grid-cols-[1fr_auto_auto] gap-4 border-b p-5 text-sm last:border-0" key={product.id}><span className="font-medium">{product.name}</span><span>{product.price.toLocaleString('ru-RU')} ₽</span><span>{product.stock} шт.</span></div>)}</div></section>; }
