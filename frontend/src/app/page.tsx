import Link from 'next/link';
import { ProductCard } from '@/components/product-card';
import { PromoCarousel } from '@/components/promo-carousel';
import { getProducts } from '@/lib/catalog-repository';

export const dynamic = 'force-dynamic';

export default async function Home() { const products = await getProducts(); return <><PromoCarousel /><section className="container-page py-20"><div className="mb-9 flex items-end justify-between gap-4"><div><p className="text-xs font-medium uppercase tracking-[.18em] text-terracotta">Выбор дизайнеров</p><h2 className="mt-2 font-serif text-4xl">Камины для вашего пространства</h2></div><Link href="/catalog" className="text-sm font-medium text-terracotta hover:underline">Весь каталог →</Link></div><div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.slice(0, 3).map((product) => <ProductCard product={product} key={product.id} />)}</div></section><section id="about" className="border-y border-ink/10 bg-white"><div className="container-page grid gap-10 py-20 md:grid-cols-2"><div><p className="text-xs uppercase tracking-[.18em] text-terracotta">Ykamina</p><h2 className="mt-3 font-serif text-4xl">Огонь, продуманный до детали</h2></div><p className="max-w-xl text-lg leading-8 text-ink/70">Подбираем камин под архитектуру дома, организуем доставку и монтаж. Мы работаем с проверенными производителями и отвечаем за результат от первого эскиза до первого огня.</p></div></section><section id="delivery" className="container-page grid gap-6 py-16 sm:grid-cols-3">{[['01', 'Поможем выбрать', 'Консультация инженера и визуализация в вашем интерьере.'], ['02', 'Доставим бережно', 'Собственная логистика по Москве и доставка по России.'], ['03', 'Установим безопасно', 'Монтаж сертифицированной командой с гарантией.']].map(([number, title, text]) => <div key={number} className="border-t border-ink/30 pt-5"><p className="text-sm text-terracotta">{number}</p><h3 className="mt-3 font-serif text-2xl">{title}</h3><p className="mt-2 text-sm leading-6 text-ink/65">{text}</p></div>)}</section></>; }
